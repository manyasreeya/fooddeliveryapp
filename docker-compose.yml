services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: fooddb
    ports:
      - "3307:3306"

  app:
    image: fooddeliveryapp
    ports:
      - "8082:8080"
    depends_on:
      - mysql